[package]
name = "projects-rust-server"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.1"
postgres = "0.19"
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres", "macros"] }
